export interface Report {
    //Propiedades que necesita nuestra interfaz
    
    documentNumber?: string; // número de documento
    paymentType?: string; // tipo de pago
    dateRegistration?: string; // fecha de registro
    saleTotal?: string; // Total de la venta
    product?: string; // Producto
    amount?: number; // Cantidad
    price?: string; // Precio
    total?: string; // Total
}
