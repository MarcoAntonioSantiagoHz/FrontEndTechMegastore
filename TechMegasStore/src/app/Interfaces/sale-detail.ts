export interface SaleDetail {
    //Propiedades que necesita nuestra interfaz
    idProduct: number;
    descriptionProduct: string;
    amount: number;
    priceText: string;
    totalText: string;
}
